REQUIRE [ELSE] ~mak/CompIF1.f

: DEFINED? ( -- f ) \ "name"
  PARSE-NAME SFIND  IF DROP TRUE ELSE 2DROP FALSE THEN
; 

VECT [DEFINED] IMMEDIATE

' DEFINED? TO [DEFINED]

: [UNDEFINED]  ( -- f ) \ "name"
  POSTPONE [DEFINED] 0=
; IMMEDIATE

: [IFDEF]
    POSTPONE [DEFINED]
    0= IF POSTPONE [ELSE] THEN
; IMMEDIATE

: [IFNDEF]
    POSTPONE [UNDEFINED]
    0= IF
 POSTPONE [ELSE]
 THEN
; IMMEDIATE

